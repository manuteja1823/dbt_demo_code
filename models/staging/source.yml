version: 2
sources:
    - name: datafeed_shared_schema
      database: "{{('SOURCE_DB')}}"
      schema: SOURCE_LAYER
      tables:
        - name: STAGE_EMP
        - name: RAW_CUSTOMERDATA
        - name: RAW_ORDER
